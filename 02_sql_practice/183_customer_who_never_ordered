/*
LeetCode: Customers Who Never Order
Difficulty: Easy
Database: PostgreSQL

Problem:
Find the names of customers who never placed any orders.

Approach:
Use NOT EXISTS to check absence of related records in Orders table.
*/

SELECT
    name AS customers
FROM customers c
WHERE NOT EXISTS (
    SELECT 1
    FROM orders o
    WHERE o.customerid = c.id
);
